import 'package:flutter/material.dart';

class TeamsPlan extends StatelessWidget {
  const TeamsPlan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tab 2 Content'));
  }
}