import 'package:flutter/material.dart';

class MatchStatistics extends StatelessWidget {
  const MatchStatistics({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tab 3 Content'));
  }
}