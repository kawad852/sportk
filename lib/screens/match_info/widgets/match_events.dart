import 'package:flutter/material.dart';

class MatchEvents extends StatelessWidget {
  const MatchEvents({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tab 1 Content'));
  }
}
